name: Paper Radar checks

on:
  pull_request:
    paths: ['apps/paper-radar/**', 'apps/ai-persona/**', 'scripts/**', 'install.sh', 'package*.json', '.github/workflows/paper-radar.yml', '.github/workflows/release.yml']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  checks:
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: '0.11.7'
          python-version: '3.12'
      - uses: actions/setup-node@v6
        with:
          node-version: '24.21.0'
      - run: node scripts/setup-paper-radar.mjs --check
      - run: uv run --no-project --python 3.12 python scripts/build-release.py
      - run: uv run --no-project --python 3.12 python scripts/test-installer.py --dist-dir dist/release
